export default class PlanetsDTO {
    constructor(name, type, diameter, terrain, population) {
      this.name = name;
      this.type = type;
      this.diameter = diameter;
      this.terrain = terrain;
      this.population = population;
    }
  }